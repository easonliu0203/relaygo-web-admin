# Web Admin 快速啟動指南

## 🚀 快速啟動

### Windows 系統

```bash
# 方法 1：使用批次檔（推薦）
cd web-admin
scripts\start-admin.bat

# 方法 2：使用 npm 命令
cd web-admin
npm run dev
```

### macOS/Linux 系統

```bash
# 使用 shell 腳本
cd web-admin
./scripts/dev.sh

# 或使用 npm 命令
cd web-admin
npm run dev
```

## ✅ 確認服務啟動成功

啟動後，您應該看到：

```
✓ Ready on http://localhost:3001
○ Compiling / ...
✓ Compiled / in XXXms
```

## 🧪 測試 API 端點

### 測試健康檢查

```bash
curl http://localhost:3001/api/health
```

預期回應：
```json
{"status":"ok","timestamp":"2025-01-01T17:00:00.000Z"}
```

### 測試價格配置端點

```bash
curl http://localhost:3001/api/pricing/packages
```

預期回應：
```json
{
  "success": true,
  "data": [
    {
      "id": "small_6h",
      "name": "3-4人座 6小時方案",
      "duration": 6,
      "originalPrice": 50,
      "discountPrice": 50,
      ...
    },
    ...
  ],
  "source": "database"
}
```

## 🔧 常見問題

### 問題 1：端口被佔用

**錯誤訊息**：
```
Error: listen EADDRINUSE: address already in use :::3001
```

**解決方法**：

**Windows**：
```bash
# 查找佔用端口的進程
netstat -ano | findstr :3001

# 結束進程（替換 PID 為實際的進程 ID）
taskkill /PID <PID> /F
```

**macOS/Linux**：
```bash
# 查找佔用端口的進程
lsof -i :3001

# 結束進程
kill -9 <PID>
```

### 問題 2：依賴未安裝

**錯誤訊息**：
```
Error: Cannot find module 'next'
```

**解決方法**：
```bash
cd web-admin
npm install
```

### 問題 3：環境變數未配置

**錯誤訊息**：
```
Error: Missing environment variables
```

**解決方法**：
```bash
# 複製環境變數範本
cp .env.example .env

# 編輯 .env 文件，填入實際的配置值
```

## 📱 配合 Android 模擬器使用

當 web-admin 服務啟動後，Android 模擬器可以通過 `10.0.2.2:3001` 訪問：

```dart
// Flutter 應用中的配置
static const String _baseUrl = 'http://10.0.2.2:3001/api';
```

測試連接：
```bash
# 從 Android 模擬器測試
adb shell curl http://10.0.2.2:3001/api/health
```

## 🛑 停止服務

在終端中按 `Ctrl + C` 停止服務。

## 📊 服務監控

查看服務日誌：
- 所有 API 請求都會在終端中顯示
- 錯誤訊息會以紅色顯示
- 成功的請求會顯示狀態碼和響應時間

## 🔗 相關端點

- **管理後台**: http://localhost:3001
- **API 基礎路徑**: http://localhost:3001/api
- **健康檢查**: http://localhost:3001/api/health
- **價格配置**: http://localhost:3001/api/pricing/packages
- **預約管理**: http://localhost:3001/api/bookings

## 📚 更多資訊

- 詳細設定指南：`web-admin/README.md`
- API 文檔：`web-admin/docs/`
- 資料庫設定：`web-admin/database/`

---

**最後更新**: 2025-01-01  
**版本**: 1.0.0
